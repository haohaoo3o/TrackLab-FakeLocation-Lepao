# 。compileSdk=35 由 AGP 8.7.2 原生支持，
# 故不设 android.suppressUnsupportedCompileSdk（依赖与配置约束：避免与 compileSdk=35 误导性不匹配）。
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
